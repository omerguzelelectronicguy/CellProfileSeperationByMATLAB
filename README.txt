Hocam,
The work on Machine learning is prepared by fixing the label. Varaible, features, has three diffrernt column which are std values average values and label. We corrected the label with hand then we run the neural network. Since a threshold even can seperate the types of cell. it is better to create a label by using threshold then fixing it. However, the parameters cannot be used for other images because the labeled images are different than the other images as it explained in the report.
main two implementation is Threshold_part and ML_part
The ML is deeply arranged in the folder ML.
Thank you.